extends ../layout/layout.pug

block content
  main
    section.search-page
      .search-page__wrapper(class= `${!searchResults.length ? `search-page__wrapper--empty` : ``}`)
        h1 Поиск
        .search.search-page__form
          form(action=`/search` method=`get`)
            label
              input(type=`text` name=`query` placeholder=`Что ищем?`)
            button.search__button.button.button--transparent(type=`submit`) Найти
          if searchResults.length
            each result in searchResults
              .search__result
                time.search__date(datetime=`${result.createdDate}`)= result.createdDate
                a.search__link= result.title
          else
            p.search-page__empty Ничего не нашлось


